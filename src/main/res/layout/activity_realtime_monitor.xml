<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#022336"
    tools:context="com.beetech.serialport.activity.RealtimeMonitorActivity">

    <com.hjq.bar.TitleBar
        android:id="@+id/tb"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="8"
        app:backButton="true"
        app:barStyle="transparent"
        app:leftBackground="@drawable/left"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rvReadDataRealtimeData"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="92"
        android:layout_marginBottom="5dp"
        android:divider="@drawable/bg_list_selector"
        android:dividerHeight="1px"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:scrollbarStyle="outsideOverlay"/>
</LinearLayout>
